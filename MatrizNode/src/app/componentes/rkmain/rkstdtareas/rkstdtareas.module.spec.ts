import { RkstdtareasModule } from './rkstdtareas.module';

describe('RkstdtareasModule', () => {
  let rkstdtareasModule: RkstdtareasModule;

  beforeEach(() => {
    rkstdtareasModule = new RkstdtareasModule();
  });

  it('should create an instance', () => {
    expect(rkstdtareasModule).toBeTruthy();
  });
});
