import { RkyriesgoresidualtableModule } from './rkyriesgoresidualtable.module';

describe('RkyriesgoresidualtableModule', () => {
  let rkyriesgoresidualtableModule: RkyriesgoresidualtableModule;

  beforeEach(() => {
    rkyriesgoresidualtableModule = new RkyriesgoresidualtableModule();
  });

  it('should create an instance', () => {
    expect(rkyriesgoresidualtableModule).toBeTruthy();
  });
});
