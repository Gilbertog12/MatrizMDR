import { RkyriesgopurotableModule } from './rkyriesgopurotable.module';

describe('RkyriesgopurotableModule', () => {
  let rkyriesgopurotableModule: RkyriesgopurotableModule;

  beforeEach(() => {
    rkyriesgopurotableModule = new RkyriesgopurotableModule();
  });

  it('should create an instance', () => {
    expect(rkyriesgopurotableModule).toBeTruthy();
  });
});
