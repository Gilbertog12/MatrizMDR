import { AddrkyseveridadModule } from './addrkyseveridad.module';

describe('AddrkyseveridadModule', () => {
  let AddrkyseveridadModule: AddrkyseveridadModule;

  beforeEach(() => {
    AddrkyseveridadModule = new AddrkyseveridadModule();
  });

  it('should create an instance', () => {
    expect(AddrkyseveridadModule).toBeTruthy();
  });
});
